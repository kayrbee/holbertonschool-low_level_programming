#include "main.h"

/**
 * mul - multiplies two digits
 * @a: first integer from main for multiplication
 * @b: second integer from main for multiplication
 * Return: product of a,b
 */
int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}

