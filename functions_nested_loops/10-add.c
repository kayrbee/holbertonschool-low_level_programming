#include "main.h"

/**
 * add - adds two integers
 * @a: integer a, passed from main for sum with
 * @b: integer b, also passed from main
 * Return: int sum of a, b
 */
int add(int a, int b)
{
	int sum;

	sum = (a + b);

	return (sum);
}

