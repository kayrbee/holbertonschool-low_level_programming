#include "main.h"

/**
 * _abs - returns the absolute value of an integer
 * @int: passed from main for evaluation
 * Return: tbc!
 */
int _abs(int)
{
	/* Define */
	int i;

	i = (unsigned int) n;

	return (i);
}

